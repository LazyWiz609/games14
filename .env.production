# Leave empty to use same-origin backend at runtime (recommended)
# If your backend is on another domain or path, set it explicitly, e.g.:
# VITE_API_BASE=https://your-prod-domain.com/backend/api
VITE_API_BASE=
